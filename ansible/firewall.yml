- name: 'Install and configure UFW firewall'
  hosts: localhost
  connection: local
  gather_facts: false
  become: true
  tasks:
    - name: 'Install UFW'
      community.general.pacman:
        name:
          - ufw
        state: present

    - name: 'Load required kernel modules for netfilter'
      community.general.modprobe:
        name: "{{ item }}"
        state: present
      loop:
        - ip_tables
        - ip6_tables
        - iptable_filter
        - ip6table_filter
      ignore_errors: true

    - name: 'Enable UFW service'
      ansible.builtin.systemd:
        name: ufw
        enabled: true
        state: started

    - name: 'Configure UFW default policies'
      community.general.ufw:
        state: enabled
        policy: "{{ item.policy }}"
        direction: "{{ item.direction }}"
      loop:
        - { direction: 'incoming', policy: 'deny' }
        - { direction: 'outgoing', policy: 'allow' }
