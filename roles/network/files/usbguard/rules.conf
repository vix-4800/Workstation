# USBGuard rules - allow all devices by default (open policy)
# To harden: run `sudo usbguard generate-policy > /etc/usbguard/rules.conf`
# which generates device-specific allow rules for all currently connected devices,
# then replace this file with the generated output and reload usbguard.
allow
