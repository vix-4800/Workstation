prefix_rule(
    pattern = ["curl"],
    decision = "forbidden",
    justification = "Blocked for safety. Use a reviewed manual workflow instead.",
)

prefix_rule(
    pattern = ["doas"],
    decision = "forbidden",
    justification = "Blocked for safety. Use a reviewed manual workflow instead.",
)

prefix_rule(
    pattern = ["git", ["am", "apply", "cherry-pick", "clean", "merge", "push", "rebase", "reset"]],
    decision = "forbidden",
    justification = "Blocked for safety. Use a reviewed manual workflow instead.",
)

prefix_rule(
    pattern = ["nc"],
    decision = "forbidden",
    justification = "Blocked for safety. Use a reviewed manual workflow instead.",
)

prefix_rule(
    pattern = ["netcat"],
    decision = "forbidden",
    justification = "Blocked for safety. Use a reviewed manual workflow instead.",
)

prefix_rule(
    pattern = ["php", "artisan", "tinker"],
    decision = "forbidden",
    justification = "Blocked for safety. Use a reviewed manual workflow instead.",
)

prefix_rule(
    pattern = ["rm"],
    decision = "forbidden",
    justification = "Blocked for safety. Use a reviewed manual workflow instead.",
)

prefix_rule(
    pattern = ["rsync"],
    decision = "forbidden",
    justification = "Blocked for safety. Use a reviewed manual workflow instead.",
)

prefix_rule(
    pattern = ["scp"],
    decision = "forbidden",
    justification = "Blocked for safety. Use a reviewed manual workflow instead.",
)

prefix_rule(
    pattern = ["sftp"],
    decision = "forbidden",
    justification = "Blocked for safety. Use a reviewed manual workflow instead.",
)

prefix_rule(
    pattern = ["ssh"],
    decision = "forbidden",
    justification = "Blocked for safety. Use a reviewed manual workflow instead.",
)

prefix_rule(
    pattern = ["sudo"],
    decision = "forbidden",
    justification = "Blocked for safety. Use a reviewed manual workflow instead.",
)

prefix_rule(
    pattern = ["wget"],
    decision = "forbidden",
    justification = "Blocked for safety. Use a reviewed manual workflow instead.",
)

prefix_rule(
    pattern = ["docker", ["kill", "restart", "rm", "rmi", "stop"]],
    decision = "forbidden",
    justification = "Blocked for safety. Use a reviewed manual workflow instead.",
)

prefix_rule(
    pattern = ["docker", "compose", ["build", "down", "restart", "up"]],
    decision = "forbidden",
    justification = "Blocked for safety. Use a reviewed manual workflow instead.",
)

prefix_rule(
    pattern = ["docker", "network", "rm"],
    decision = "forbidden",
    justification = "Blocked for safety. Use a reviewed manual workflow instead.",
)

prefix_rule(
    pattern = ["docker", "system", "prune"],
    decision = "forbidden",
    justification = "Blocked for safety. Use a reviewed manual workflow instead.",
)

prefix_rule(
    pattern = ["docker", "volume", "rm"],
    decision = "forbidden",
    justification = "Blocked for safety. Use a reviewed manual workflow instead.",
)
