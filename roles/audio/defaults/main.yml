---
audio_packages:
  - pipewire
  - pipewire-alsa
  - pipewire-pulse
  - wireplumber
  - pavucontrol

multimedia_packages:
  - ffmpeg
  - vlc
  - mpv
  - imagemagick

audio_configs:
  - { src: pipewire, dest: "{{ home }}/.config/pipewire" }
  - { src: wireplumber, dest: "{{ home }}/.config/wireplumber" }
